require "spec_helper"

describe Officesimplify do
  it "has a version number" do
    expect(Officesimplify::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
